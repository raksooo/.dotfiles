#!/bin/sh

if [ $1 ] && [[ $1 =~ magnet:.* ]]; then
    magnet=$1
    if [ $2 ]; then
        subs="-- --sub-file=$2"
    fi
else
    magnet=$(xclip -selection clipboard -o)
    if [ $1 ]; then
        subs="-- --sub-file=$1"
    fi
fi

peerflix $magnet --mpv $subs

