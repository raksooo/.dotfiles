#!/bin/sh

if [[ $1 ]]; then
    magnet=$1
else
    magnet=$(xclip -selection clipboard -o)
fi
url=http://192.168.1.223:8888/

peerflix $magnet > /dev/null 2>&1 &
sleep 12 && mpv $url > /dev/null 2>&1 && killall peerflix &

